{
  "name": "50-seeds",
  "version": "1.2.0",
  "private": true,
  "description": "50 categories of popular visual seeds for AI image creation",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-data.mjs",
    "validate": "node scripts/validate-data.mjs",
    "test:combiner": "node scripts/test-combiner.mjs",
    "test": "npm run generate && npm run test:combiner && npm run validate",
    "start": "npx --yes serve . -l 4173"
  },
  "keywords": [
    "ai-art",
    "prompt",
    "visual-seeds",
    "json"
  ],
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
